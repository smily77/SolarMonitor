This is a Display for the Huawei Sun2000
-> Programming Arduino IDE (Note the .ino of the mainprogram must be in a folder with the dame name)
-> All secret data is fetched by including the Credentials.h
-> Ther is one poller retrives the information from thze SUN2000 and display it and the are clients only display the same. They get the information by UDP. This is because only one should poll the data from the solar controller as  can only proper deal with on requester at the time)